import { Tooltip, TooltipContent, TooltipProvider, TooltipTrigger } from "@radix-ui/react-tooltip";

interface HintProps{

    label: String,
    
}

const Hint = () => {
    return (  );
}
 
export default Hint;