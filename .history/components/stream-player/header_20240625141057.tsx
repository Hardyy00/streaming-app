"use client";

interface HeaderProps {
  hostName: string;
  hostIdentity: string;
  viewerIdentity: string;
  isFollowing: boolean;
}

export const Header: React.FC = () => {
  return <div>Header</div>;
};
