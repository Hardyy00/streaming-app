"use client";

import { useState } from "react";

interface ChatFormProps {
  onSubmit: () => void;
  value: string;
  isHidden: boolean;
}

export const ChatForm = () => {
  return <div>Chat Form</div>;
};
